{
  "name": "life-rpg",
  "version": "1.0.0",
  "private": true,
  "description": "Life RPG - Cozy Lo-Fi Study Room Edition (Monorepo)",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "node server/index.js",
    "seed": "cd server && node utils/seedData.js"
  }
}
